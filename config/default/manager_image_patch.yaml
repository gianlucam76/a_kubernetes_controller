apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: inventory-controller-system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: docker.io/my-registry/inventory:main
        name: manager

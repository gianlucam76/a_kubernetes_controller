apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: inventory-controller-system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: 
